# DO NOT commit real values. Use sealed-secrets, External Secrets Operator,
# or a secrets manager (Vault, AWS Secrets Manager) in production.
# This file exists only as a reference showing the expected keys.
apiVersion: v1
kind: Secret
metadata:
  name: agent-secrets
  namespace: ai-agents
type: Opaque
stringData:
  openai-api-key: "REPLACE_WITH_REAL_KEY"
  jwt-secret: "REPLACE_WITH_256_BIT_SECRET"
